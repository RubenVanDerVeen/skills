# Editor swap files
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE (uncomment as needed)
# .vscode/
# .idea/

# Local opencode config (machine-specific: permission overrides, MCP secrets).
# Root opencode.json IS tracked: it ships the sandbox-wide defaults (notably
# subagent_depth) to every clone via the nas-repos-fetch bundle rsync.
opencode.jsonc
.opencode/
!opencode.json
