# Skill Harvest Report - 2026-07-12

Third harvest run. FIFO backlog drain continues from the 2026-06-14 cursors.

## Run metadata

| Field | Value |
|---|---|
| Date | 2026-07-12 |
| State file | present; cursors from the 2026-07-07 run |
| Since cutoff, Claude Code (UTC) | 2026-06-14T12:38:44Z |
| Since cutoff, opencode (UTC) | 2026-06-14T21:21:14Z |
| Sources processed | Claude Code, opencode |
| Sources skipped | none |

## Session counts

| Source | In window | Processed (oldest-first, cap 40) | Backlog remaining |
|---|---|---|---|
| Claude Code | 92 | 39 (2026-06-14 to 2026-06-28; first enumerated file's mtime equalled the cursor = already processed, skipped) | 52 |
| opencode | 184 | 40 (2026-06-14 to 2026-06-26) | 144 |

Malformed JSONL lines skipped: 0. Digests only per `references/extraction.md`; long user messages truncated at 1200 chars in the digest.

Note: many late-June sessions in this batch were already digested by the newest-first 2026-07-03 run; findings already reported and applied there (/full-cycle, execute-plan momentum, altium logging taxonomy, eval-dir exclusions) were checked against the current repo and are not re-proposed.

## Findings

### Finding 1 - fix-skill: rubens-project-standardization migration deletes framework artifact dirs instead of moving them

`references/artifacts.md:205` (anti-patterns) instructs: "A `docs/superpowers/` ... directory ever landing in the repo ... `git rm` it, no exceptions." Correct for a fresh miss, destructive for a migration: applying standardization to an existing project with a populated `docs/superpowers/` made the agent delete real specs/plans, and the user had to ask for a restore.

**Evidence:**
- OC `ses_0ff553f86` (hermes-console, 2026-06-25): "superpowers/ is still a dir. it should just be artifacts from now on" → agent removed the files → "i meant not completely delete the files, but the files should live under artifacts. can you restore?"

**Target:** `skills/rubens-project-standardization/references/artifacts.md` anti-patterns section.
**Edit sketch:** replace the "`git rm` it, no exceptions" line with: pre-existing content is moved (`git mv` contents into `docs/artifacts/{specs,plans,reviews}/`, preserving dates/topics), then the empty framework dir is removed; `git rm` applies only to true duplicates that already exist at the canonical path.

### Finding 2 - fix-skill: AGENTS templates lack a commit-granularity rule (agent commits+pushes every small tweak)

The templates cover *when* to commit (never without instruction; plan-execution carve-out) but not *how much* per commit. In interactive sessions the agent committed and pushed after each micro-change until told to stop.

**Evidence (3 sessions):**
- OC `ses_1169b0e52` (hermes-console): "no need to commit and push every small change. try to bundle more changes into a single commit."
- CC `hobby-homelab/7baf8fda` : "bundle relevant items together in a single commit and write a descriptive commit"
- CC `aardbei-plukkers/ec8f8985`: "can you write a commit message that covers the entire diff?"

**Target:** `skills/rubens-project-standardization/templates/AGENTS-{small,medium,large}.md` git-rules block.
**Edit sketch:** add one line: "Bundle related changes into a single commit; don't commit/push per tweak. One logical change = one commit."

### Finding 3 - fix-skill: drawio-pro assumes the desktop app is installed

The skill hardcodes the desktop install (`SKILL.md:482`: `C:\Program Files\draw.io\draw.io.exe`; `SKILL.md:49`: magic `host="65bd71144e"` matching "the user's drawio desktop install"). On a machine/context without the app the flow broke and the user interrupted.

**Evidence:**
- CC `projects/ba9bf09b` (2026-06-27): user interrupts the skill run: "no local draw.io".
- CC `tools-skills/5d1272f1` (same day): the preceding drawio-pro tuning session, aborted mid-flow.

**Target:** `skills/drawio-pro/SKILL.md`.
**Edit sketch:** in the final open/export step, check availability first (`Test-Path` the exe / `npx @drawio/cli` fallback); if neither exists, deliver the `.drawio` file and say how to open it, don't fail the flow. Keep the `host` attribute unconditionally (it's just a diff-stability constant).

### Finding 4 - config: user audits which skills were used ("what skills did you use?")

**Evidence (2 sessions):** OC `ses_13a5b7b79`, OC `ses_1367599c1` (both hermes-console): "what skills did you use during this session" / "what skills did you use?"

Flag only per the skill's config rule: could become a hook or a one-line end-of-run note listing invoked skills. Not implemented; noted for the user.

### Finding 5 - project suggestion (not this repo): hermes-console should pin its mount/compose map in project context

The user re-pastes the gateway/console mount lists and the compose location (`Homelab/generaldock/docker-compose.yml`) session after session; agents repeatedly guess NAS paths wrong (`/mnt/nas/workspace` vs `07_agent-workspace`, artifacts layout).

**Evidence (4+ sessions):** OC `ses_11594e82c` (mount list pasted), OC `ses_113e1bda3` (compose pasted twice), OC `ses_10a285095` (compose screenshot + double-mount cleanup), CC `hermes-console/a3b35ef4` (mounts re-derived via docker inspect relay).

**Action:** belongs in the hermes-console repo (`.agents/` infra doc per the standardization layout). Listed for visibility only, same as the 2026-07-07 deploy-runbook pointer.

### Considered and rejected

- **"research > spec > plan > subagent implementation in one pass" (many sessions)**: already harvested 2026-07-07; `/full-cycle` exists.
- **"continue" pumping during plan execution**: already applied to `commands/execute-plan.md`.
- **Altium flows dictated into the skill** (shelve pours, irregular-outline pour, stencil Gerber X2, PnP output, board resize, mils→mm): all verified present in `skills/altium-pro/`, applied in-session at the time.
- **"academic-tools is legacy" / typst-tools import**: covered by `typst-pro` SKILL.md + `references/migration.md`.
- **"check the quality of the latest features and fix where needed" (CC homelab `6a6a1e29`, CC werkbank `6c5cd0fc`)**: 2 sessions, but the `code-reviewer` agent already covers it; a wrapper command adds nothing.
- **Battery-percentage math, hermes write-safe-root, session-loading latency**: one-off project bugs, fixed in-session.

## State file

Written after approval step (FIFO cursors = newest session processed per source):

- `claude-code`: `2026-06-28T12:51:48Z`
- `opencode`: `2026-06-26T22:05:21Z` (ms-epoch 1782511521178)

## Outcome (post-approval)

- Finding 1: **applied.** `references/artifacts.md` anti-pattern rewritten to move-not-delete (`git mv` into `docs/artifacts/`, `git rm` only true duplicates). Synced to `~/.claude/skills/rubens-project-standardization/`.
- Finding 2: **applied.** Bundling rule added to all three AGENTS templates' git-rules block. Synced to `~/.claude/skills/`. (opencode skills dir only carries graphify; nothing to sync there.)
- Finding 3: **skipped** (not selected).
- Findings 4-5: flags/pointers only, no edits by design.

Nothing committed; awaiting user instruction per AGENTS.md.
