# SP-4 Go Plan

## References

- Spec: `docs/artifacts/specs/code-standardization/2026-08-04-sp4-go-design.md`
- Frozen template: `2026-08-04-foundation-design.md` § "Frozen per-language template"

## Branch

Lands on `feat/code-std` after foundation. Additive file.

## Tasks

### SP4-T1: Write `references/go.md`

Instantiate the frozen 8-section template with the Go picks. Lean where Go decides for us; explicit where there is a real choice (e.g. `golangci-lint` rule set). 8 sections in order; each ends with the standardizer-check note.

**Verify**: 8 sections in frozen order; check command `gofmt -l . && golangci-lint run`; `internal/` named as the boundary mechanism; no em-dashes.

**Commit**: `feat(skills): add go code-standardization guide`

### SP4-T2: Verify

- Em-dash scan empty.
- SKILL.md dispatch link `references/go.md` resolves.
- Arch note consistent with `references/architecture.md` (compiler-enforced no-cycles, `internal/` boundary).

**Commit**: none.
